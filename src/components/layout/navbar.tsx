/**
 * Navbar global del sitio.
 * Usa el header de marca MUNAY (búsqueda, badge LIVE, "Publicar prenda").
 */
import { MunayHeader } from '@/components/munay/header'

export function Navbar() {
  return <MunayHeader />
}
